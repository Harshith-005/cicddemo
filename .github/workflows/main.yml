name: simple-workflow

on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: MY first step
        run: 
          echo "hello world"
          echo "hi"
      - name: Check doker
        run: |
          doker 
          dokcer images
      - name: Run docker file 

        run:
          docker run hello-worls

      
              

         


        

